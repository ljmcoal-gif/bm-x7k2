# 덱 생성 의존성 (Node)

전역 설치:
```bash
npm install -g pptxgenjs
```
실행 시 `NODE_PATH=$(npm root -g)` 로 전역 모듈을 참조한다.

PDF 변환·폰트는 LibreOffice(soffice)와 ~/.fonts의 IBM Plex / Noto Serif CJK KR 필요.
폰트 설치는 ../CLAUDE.md 의 "디자인 시스템" 절 참조.
